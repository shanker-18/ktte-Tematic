# Local Development
DB_NAME=asset_management
DB_USER=postgres
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432

# Production (Render provides DATABASE_URL automatically)
# DATABASE_URL=postgres://user:password@host:5432/database

# Application
PORT=3000
NODE_ENV=development

# Session Secret (generate random string for production)
SESSION_SECRET=asset-management-secret-key
